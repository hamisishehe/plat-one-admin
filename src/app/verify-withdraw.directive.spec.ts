import { VerifyWithdrawDirective } from './verify-withdraw.directive';

describe('VerifyWithdrawDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyWithdrawDirective();
    expect(directive).toBeTruthy();
  });
});
